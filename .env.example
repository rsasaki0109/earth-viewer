# Optional: Cesium ion access token. Leave empty to run without ion (OSM imagery + ellipsoid terrain only).
VITE_CESIUM_ION_TOKEN=

# Base path used by Vite/GitHub Pages. Defaults to /earth-viewer/ if unset.
VITE_BASE=/earth-viewer/
